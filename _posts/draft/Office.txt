# Excel

Excel打开csv乱码：使用U8-with-BOM保存csv

SUMIF(
    判断的区域,
    条件如">5",
    求和的区域，高度和判断的区域一样，可省则用第一个参数)

VLOOKUP(想查找的值单个单元格,
    要查找的区域，第一个参数会在本区域的第一列里去查找,
    返回值所在列的序号，如2表示区域里的第二列,
    0表示精确匹配，默认为1表示近似匹配)

手动版VLOOKUP，用于查找的值不在区域的第一列，或者说需要的值的所在列在查找的值的列之前：
INDEX(要查找的区域, MATCH(想查找的值单个单元格, 在哪一列中查找前一个参数, 0表示精确匹配)返回行号, 需要的值所在的列相比于区域第一列的序号)

IF(D2="Value", "真值", "假值")

COUNTIF(区域, 条件) 条件可以是 数字 文本 单元格引用 表达式如">20"。前三者就是判断相等

字符串拼接：&。取子串：MID(text, start, num)

转换：TEXT(值, 格式如"m"表示取月份)

制作下拉列表（限制可选的值）：数据-数据验证-允许序列，设置来源

合并多个文件：数据 - 获取数据 - 自文件 - 从文件夹

引用另一工作表中的单元格：在前面加 表名!

ROUND、ROUNDUP、ROUNDDOWN：位数用0表示舍入到整数，用-1表示到十位数

更改单元格样式显示重点：开始-条件格式。如果是数据范围，就用“突出显示单元格规则”，还可以显示重复值；如果已经排好序，用“最前/最后规则”

数据透视表：可以挑选需要的行、列、值，生成表，还能再生成图表。

数据：
快速填充(Ctrl+E)类似于某种模式匹配。
删除重复值：按列考虑重复值。“扩展选定区域”会删除重复项的那一行，“以当前选定区域排序”只删除单元格
分类汇总：类似于groupby，会在同值数据下方显示。要先排序
分列：相当于split()，也可按固定宽度分（类substr）

日期：
TODAY()返回当前的日期
NOW()返回当前的日期和时间，并在工作表每次刷新时更新
DATEDIF(开始日期, 结束日期, 间隔单位) 计算两个日期之间的差，按指定单位返回，如D表示天数之差，MD表示忽略年月只看天的差

选中所有空值：Ctrl+G定位
跳转到最后一个非空单元格：Ctrl+方向键
选中区域中行内容不一致的单元格：Ctrl+\
求和：Alt+=

视图 - 冻结首行：指的是当前屏幕显示区域中的首行。冻结窗格：会冻结选中了的左上方的行列

页面布局：横向。高度、宽度：一般一个选自动，另一个选1页。每一张纸都出现标题：打印标题 - 顶端标题行，横向时选第一列

https://www.youtube.com/watch?v=Vl0H-qTclOg

# PowerPoint

选择重叠对象：开始 - 右边的选择 - 选择窗格

文字输出到Word：文件 - 导出 - 创建讲义 - 只使用大纲。但实测是空白的

将多个图片插入PPT，一整页一张：插入 - 相册。提取所有图片：改为zip，在media文件夹里

主题：设计。右边“变体”可统一调整颜色、字体等

字体嵌入文件：文件 - 选项 - 保存。启用后再保存一次PPT

SmartArt：先插入文本框，给二级文本用tab缩进。选择文本框或文本，开始 - 转换为SmartArt

单屏进入演示模式：Alt + F5

## 母版

视图 - 幻灯片母版；在最上面的母版里添加图片会应用到所有幻灯片里；选某版式，第一个菜单，勾上“隐藏背景图形”可在该版式里隐藏母版的背景。

下面是版式，相当于子模板。使用：开始 - 第二块“幻灯片”菜单 - 版式。

跨PPT文件复制粘贴，默认不复制母版。选择“保留源文件”可复制版式库。

## 图片格式

颜色 - 重新着色

压缩图片：包括删除裁剪区域、更改分辨率。可应用于单个或所有图片

裁剪 - 裁剪为形状。如平行四边形。F4：重复上一步操作

安排多个图片：选中 - 图片版式。其实就是把图片填充进SmartArt占位符里

背景：设计 - 设计背景格式 - 图片 - 插入 - 下方应用到全部。
如果勾上“将图片平铺为纹理”，则当图片小于幻灯片大小时会重复，一般只有专用于平铺的图片才用。
取消勾选时，会保持图片缩放比，按长宽中的一边适配到幻灯片大小，选取图片中间的区域。选项中的“偏移”实际上是拉伸/压缩图片，而不是平移；解决方法：先普通地插入图片，放到想要的位置后裁剪，再复制到剪贴板，插入时选择剪贴板。

## 快捷键

复制：Ctrl + 左键。平行移动、按比例缩放：Shift。快捷键版格式刷：Ctrl + Shift + C/V
段落居中对齐：Ctrl + E
组合对象：Ctrl + G。取消组合：Ctrl + Shift + G

## 素材

* 图片
  * https://pixabay.com/zh/
  * https://www.pexels.com/zh-cn/
  * https://unsplash.com/
  * https://www.freeimages.com/cn
  * https://magdeleine.co/ 以户外摄影为主
  * https://picjumbo.com/
  * https://pxhere.com/
  * https://www.hippopx.com/
* 图标
  * https://roundicons.com/vector-free-icons/
  * https://undraw.co/illustrations 单人画的比较具体的动作
  * https://www.streamlinehq.com/free
  * https://igoutu.cn/
  * https://www.iconfont.cn/ 阿里的
  * 品牌logo：https://www.60logo.com/ https://svglogo.top/ https://worldvectorlogo.com/zh
  * https://pictogram2.com/?lang=en 人类形象图形
* 模板
  * https://www.officeplus.cn/
  * https://www.islide.cc/ppt/template?filters=content-category.free
  * https://www.51pptmoban.com/

## 参考

* 和秋叶一起学
